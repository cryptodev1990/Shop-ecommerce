export class ProductsList {
  constructor(
    public readonly productImage: string,
    public readonly productName: string,
    public readonly productPrice: string,
    public readonly productCount: string,
  ) {
  }
}
