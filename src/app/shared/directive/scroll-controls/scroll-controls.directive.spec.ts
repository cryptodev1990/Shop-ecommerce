import { ScrollControlsDirective } from './scroll-controls.directive';

describe('ScrollControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollControlsDirective();
    expect(directive).toBeTruthy();
  });
});
