import { CopyingDirective } from './copying.directive';

describe('CopyingDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyingDirective();
    expect(directive).toBeTruthy();
  });
});
